# BioDiscover - Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core ──────────────────────────────────────────────────────────
requests>=2.31.0          # HTTP API calls (UniProt, NCBI, PDB, KEGG)
urllib3>=2.0              # HTTP transport

# ── GUI ───────────────────────────────────────────────────────────
customtkinter>=5.2.0      # Beautiful modern Tkinter widgets (optional but recommended)
pillow>=10.0.0            # Image handling in GUI (PIL)

# ── Data & Science ────────────────────────────────────────────────
biopython>=1.81           # Sequence parsing, BLAST, GenBank, PDB I/O
numpy>=1.24               # Array operations for sequence analysis
pandas>=2.0               # Tabular data, export to CSV/Excel
matplotlib>=3.7           # Plotting (GC content, protein properties, etc.)
scipy>=1.11               # Statistical analysis

# ── Bioinformatics ────────────────────────────────────────────────
# (all free, open-source)

# ── 3D Visualization ──────────────────────────────────────────────
# py3Dmol is a JavaScript viewer — we open browser links instead.
# If you want local rendering, install:
# pymol-open-source  (conda: conda install -c conda-forge pymol-open-source)

# ── Utilities ─────────────────────────────────────────────────────
tqdm>=4.66                # Progress bars for long operations
python-dotenv>=1.0        # Load .env config files
colorama>=0.4.6           # Terminal colors (for CLI mode)
loguru>=0.7               # Better logging

# ── Export ────────────────────────────────────────────────────────
openpyxl>=3.1             # Export results to Excel
reportlab>=4.0            # PDF report generation (optional)

# ── Optional: Sequence Alignment ──────────────────────────────────
# clustalw (system install) or:
# muscle>=5.1             # Multiple sequence alignment
